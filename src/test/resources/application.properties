
#used so that TestFileStorageRepository can work
quarkus.arc.exclude-types=com.cleanai.modules.files.infrastructure.persistence.S3FileStorageRepository,com.cleanai.modules.files.infrastructure.persistence.TigrisFileStorageRepository
# application.properties for test

# Disable OIDC in tests - use JWT security annotations instead
%test.quarkus.oidc.enabled=false
# AI Configuration
app.ai.gemini.api-key=${GEMINI_API_KEY:nothing}
